function countVowels(str) {

    var vowels = ['a', 'e', 'i', 'o', 'u']

    var count = 0

    for(var i = 0; i <= string.length - i ; i++) {

        if (vowels.includes(str[i])) {
        count ++
        }
    }
    console.log('number of vowels', vowelCount)
    
}

countVowels([h, a, i, k, u])